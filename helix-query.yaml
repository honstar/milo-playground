version: 1
indices:
  default:
    include:
      - /fragments/cards/**
    target: /query-index-cards.xslx
    properties:
      title:
        select: head > meta[property="og:title"]
        value: attribute(el, "content")
      cardClasses:
        select: main div.merch-card
        value: attribute(el, "className")
      cardContent:
        select: main > div
        value: innerHTML(el)
      tags:
        select: head > meta[name="cardtags"]
        value: attribute(el, "content")
